import React from 'react';
import Header from './Header';

const Categories = () => (
  <div>
    <Header />
    <section className="categoriesContainer">
      <h2 className="categoriesTitle">Under construction...</h2>
    </section>
  </div>
);

export default Categories;
